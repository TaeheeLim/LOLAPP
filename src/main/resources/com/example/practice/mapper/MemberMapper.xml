<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.practice.mapper.MemberMapper">
    <select id="findAll" resultType="Member">
        SELECT * FROM member;
    </select>

    <select id="selectMember" parameterType="String" resultType="Member">
        SELECT *
          FROM member
         WHERE MEM_IDX = #{memIdx};
    </select>

    <select id="selectMemberByEmail" parameterType="String" resultType="Member">
        SELECT *
          FROM member
         WHERE MEM_EMAIL = #{memEmail};
    </select>

    <insert id="insertMember" parameterType="Member" useGeneratedKeys="true" keyColumn="MEM_IDX" keyProperty="memIdx">
        INSERT INTO member(MEM_ID, MEM_NICK, MEM_PASSWORD, MEM_EMAIL, MEM_CELL, MEM_ROLE, SIGN_DATE)
        VALUES(#{memId}, #{memNick}, #{memPassword}, #{memEmail}, #{memCell}, #{memRole}, SYSDATE());
        <selectKey keyColumn="MEM_IDX" keyProperty="memIdx" resultType="String" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
    </insert>

    <select id="checkIsMemberIdExist" parameterType="String" resultType="int">
        SELECT COUNT(*)
          FROM member
        WHERE MEM_ID = #{memId};
    </select>

    <select id="checkIsMemberNickExist" parameterType="String" resultType="int">
        SELECT COUNT(*)
          FROM member
        WHERE MEM_NICK = #{memNick};
    </select>
</mapper>